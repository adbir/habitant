/// Copy this file to supabase_config.dart and fill in your project credentials.
///
/// Find these values in the Supabase dashboard under Project Settings → API.
/// supabase_config.dart is gitignored — never commit real credentials.

/// Supabase project credentials.
///
/// The anon key is publicly safe — Supabase security is enforced server-side
/// via Row Level Security policies, not by keeping this key secret.
const supabaseUrl = 'https://your-project-ref.supabase.co';
const supabaseAnonKey = 'your-anon-key';
